import React from 'react';
import { render } from '@testing-library/react';
import <%= name.toUpperCase() %> from './index';

describe('renders <%= name.toUpperCase() %> component', () => {
  it('renders', () => {
    render(<<%= name.toUpperCase() %> />);
    // add assertions here
  });
});
